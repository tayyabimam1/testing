export const predict = (file) => {
  return fetch('https://tayyabimam-deepsight-api.hf.space/predict', {
    method: 'POST',
    body: file
  });
};

export const predict = (file) => {
  return fetch('https://tayyabimam-deepsight-api.hf.space/predict', {
    method: 'POST',
    body: file
  });
};
